/**
 * Infrastructure layer implementations
 * 
 * This layer contains concrete implementations of the ports defined in the application layer.
 * It handles external concerns like API calls, DOM manipulation, and platform-specific code.
 */

// Logseq-specific implementations
export * from './logseq';

// Frontend implementations
export * from './frontend';